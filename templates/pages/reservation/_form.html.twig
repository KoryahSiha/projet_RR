{# 'novalidate' permet de désactiver côté client la validation HTML du formulaire.
La désactivation est utilise pour tester les formulaires et les erreurs côté serveur. #}
{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
    <div class="form-group">
		{{ form_label(form.nom) }}
		{{ form_widget(form.nom) }}
		<div class="form-error">
			{{ form_errors(form.nom) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.description) }}
		{{ form_widget(form.description) }}
		<div class="form-error">
			{{ form_errors(form.description) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.date_debut) }}
		{{ form_widget(form.date_debut) }}
		<div class="form-error">
			{{ form_errors(form.date_debut) }}
		</div>
	</div>
    
    <div class="form-group">
		{{ form_label(form.duree) }}
		{{ form_widget(form.duree) }}
		<div class="form-error">
			{{ form_errors(form.duree) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.date_fin) }}
		{{ form_widget(form.date_fin) }}
		<div class="form-error">
			{{ form_errors(form.date_fin) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.nombre_participant) }}
		{{ form_widget(form.nombre_participant) }}
		<div class="form-error">
			{{ form_errors(form.nombre_participant) }}
		</div>
	</div>

    <div class="form-group">
		{{ form_label(form.salle) }}
		{{ form_widget(form.salle) }}
		<div class="form-error">
			{{ form_errors(form.salle) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.type_reservation) }}
		{{ form_widget(form.type_reservation) }}
		<div class="form-error">
			{{ form_errors(form.type_reservation) }}
		</div>
	</div>

	<div class="form-group">
		{{ form_label(form.gestionnaire_salle) }}
		{{ form_widget(form.gestionnaire_salle) }}
		<div class="form-error">
			{{ form_errors(form.gestionnaire_salle) }}
		</div>
	</div>

    <button class="btn btn-primary mt-4 btn-sm mb-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
