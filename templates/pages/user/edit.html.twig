{% extends 'base.html.twig' %}

{% block title %}Éditer l'utilisateur{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Éditer l'utilisateur</h1>

        {{ include('pages/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        {{ include('pages/user/_delete_form.html.twig') }}

        <div class="mt-4">
            <a href="{{ path('app_user_index') }}" class="btn btn-light btn-sm mb-4">retour à la liste</a>
        </div> 
    </div>
{% endblock %}
