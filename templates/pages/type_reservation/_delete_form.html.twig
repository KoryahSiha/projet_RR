<form method="post" action="{{ path('app_type_reservation_delete', {'id': type_reservation.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer ce type de réservation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_reservation.id) }}">
    <button class="btn btn-danger mt-4 btn-sm">Supprimer</button>
</form>
