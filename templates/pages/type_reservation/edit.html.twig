{% extends 'base.html.twig' %}

{% block title %}Modification type de réservation{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Modification du type de réservation</h1>

        {{ include('pages/type_reservation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        {{ include('pages/type_reservation/_delete_form.html.twig') }}

        <div class="mt-4">
        <a href="{{ path('app_type_reservation_index') }}" class="btn btn-light btn-sm mb-4">retour à la liste</a>
        </div>

    </div>
{% endblock %}
