<form method="post" action="{{ path('app_gestionnaire_salle_delete', {'id': gestionnaire_salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestionnaire_salle.id) }}">
    <button class="btn">Supprimer</button>
</form>
