{% extends 'base.html.twig' %}

{% block title %}GestionnaireSalle index{% endblock %}

{% block body %}
    <h1>GestionnaireSalle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for gestionnaire_salle in gestionnaire_salles %}
            <tr>
                <td>{{ gestionnaire_salle.id }}</td>
                <td>{{ gestionnaire_salle.nom }}</td>
                <td>{{ gestionnaire_salle.prenom }}</td>
                <td>
                    <a href="{{ path('app_gestionnaire_salle_show', {'id': gestionnaire_salle.id}) }}">show</a>
                    {% if is_granted('ROLE_ADMIN') or app.user.gestionnaire_salle.id == gestionnaire_salle.id %}
                    <a href="{{ path('app_gestionnaire_salle_edit', {'id': gestionnaire_salle.id}) }}">edit</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
